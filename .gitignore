# .gitignore for Neovim / Lua dotfiles
# Purpose: ignore editor/OS temp files, build artifacts, and common plugin caches

# --- OS ---
.DS_Store
Thumbs.db
desktop.ini

# --- Editor / temp files ---
*.swp
*.swo
*.swn
*~
*.tmp
*.temp
*.bak
*.orig
*.log

# Vim/Neovim undo / session / view files
*.un~
Session.vim
session.*
view.*

# --- Neovim / Lua specific ---
# compiled Lua bytecode
*.luac

# plugin manager compiled files (packer, etc.)
packer_compiled.lua
plugin/packer_compiled.lua

# vendorized plugin directories (if you vendor plugins into repo accidentally)
site/pack/
pack/

# cache and local runtime dirs (usually outside repo, but sometimes symlinked)
.cache/
.local/
target/

# --- Node / JS ---
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# --- Python ---
__pycache__/
*.py[cod]
*.pyo

# --- Build / distribution ---
dist/
build/
coverage/

# --- IDEs ---
.vscode/
.idea/
*.sublime-workspace
*.sublime-project

# --- Misc ---
tags
tags.lock
*.ctags

# --- Sensitive / env ---
.env
.env.*
secret-*.json

# End of .gitignore
